<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>escaleSolutionAssignmentByHimanshuGupta</title>
</head>

<body>
    <h2>
        <%= title %>
    </h2>
    <ul>
        <li>Name: <%= user.name %>
        </li>
        <li>Email: <%= user.email %>
        </li>
        <li>Bio: <%= user.bio %>
        </li>
        <li>Businesses:
            <% if (user_business.length===0) { %>
                Currently, there are no businesses associated with this user
                <% } else { %>
                    There are following businesses:
                    <% for( let i=0; i < user_business.length; i++ ) { %>
                        <ol>
                            <li> <a href="<%= user_business[i].url %>" target="_blank">
                                    <%= user_business[i].name %>
                                </a></li>
                        </ol>
                        <% } %>
                            <% } %>

        </li>
        <li>
            <% if (user_products.length===0) { %>
                Currently, this user has not created any products.
                <% } else { %>
                    Products: There are following products:
                    <% for( let i=0; i < user_products.length; i++ ) { %>
                        <ol>
                            <li>
                                <a href="<%= user_products[i].url %>">
                                    <%= user_products[i].name %>
                                </a>
                            </li>
                        </ol>
                        <% } %>
                            <% } %>

        </li>
    </ul>
    <hr>
    <div>
        <a href="/catalog/business/create/<%= user._id %> " target="_blank">Create a Business</a>
    </div>
    <br>
    <div>
        <a href="/catalog/product/user/create/<%= user._id %> " target="_blank">Create a Product</a>
    </div>
    <hr>

    <div>
        <a href="/">Home</a>
    </div>

</body>

</html>